# Art News — Project Context

> این فایل مرجع اصلی وضعیت فعلی پروژه و handoff بین گفت‌وگوهاست. جزئیات تخصصی
> در `docs/` نگهداری می‌شوند و این فایل باید خلاصه و به‌روز بماند.

آخرین به‌روزرسانی محتوایی: ۲۰۲۶-۰۹-۱۶
## 1. شیوه همکاری

- زبان توضیحات فارسی است و نام‌های فنی و کد انگلیسی باقی می‌مانند.
- HotelYab فقط مرجع read-only معماری است و هیچ تغییری در آن انجام نمی‌شود.
- تمام تغییرات این پروژه داخل repository فعلی انجام می‌شوند.
- قبل از تغییر معماری بزرگ هماهنگی انجام می‌شود.
- سادگی و خوانایی بر abstraction زودهنگام اولویت دارد.
- بدون درخواست صریح کاربر push انجام نمی‌شود.
- کامنت داخل کد، در صورت نیاز، فقط انگلیسی نوشته می‌شود.
- بعد از هر تغییر مشخص می‌شود کدام بخش این فایل و کدام فایل‌های `docs/` باید
  به‌روزرسانی شوند.

## 2. هدف محصول

Art News نام فنی پروژه رسانه خبری فارسی و RTL «سینما نمایش» است. دامنه محصول
سینما، تئاتر، تلویزیون، شبکه نمایش خانگی و سینمای جهان است. سینما نمایش خبرها را
از طریق پنل مدیریت ثبت، بازبینی، زمان‌بندی و منتشر می‌کند. خواندن محتوا عمومی
است و حساب کاربری مخاطب برای MVP ضروری نیست. نام فنی repository همچنان
`Art-News` باقی می‌ماند.

## 3. وضعیت فعلی

پایه فنی، رابط عمومی و MVP پنل سینما نمایش پیاده‌سازی شده‌اند. پروژه اکنون monorepo
فعال با Next.js frontend، NestJS API، Prisma و SQLite دارد. صفحه اصلی،
دسته‌بندی، جزئیات خبر و جست‌وجو داده منتشرشده را از API می‌گیرند. پنل `/admin`
ورود امن، فهرست و فیلتر خبرها، ساخت و ویرایش، Draft/Review/Schedule/Publish،
دسته‌بندی، تگ، منبع، SEO، تصویر و چینش اسلایدر صفحه اول را مدیریت می‌کند. ۹ تصویرسازی نمایشی
اختصاصی به‌صورت WebP محلی جایگزین تصویرهای remote قالب شده‌اند و دو خبر نمونه
ارائه‌شده توسط کاربر نیز با تصویر محلی و متن کامل در سایت قرار دارند. هدر
دسکتاپ ناوبری افقی دسته‌بندی‌ها را کنار لوگو نمایش می‌دهد و منوی همبرگری
باریک با نوارهای رنگی موضوعی به موبایل محدود است. جست‌وجوی بازشونده در هر دو اندازه در همان هدر
قابل استفاده است و جایگاه‌های نمایشی اکران نیوز و تبلیغات در صفحه اصلی،
دسته‌بندی و خبر وجود دارند.
لوگوی رسمی «سینما نمایش» از فایل محلی در هدر نمایش داده می‌شود و فوتر به صفحه
مجوز پایگاه خبری و مهر اعتماد رسمی رسانه با شناسه گواهی `101661` متصل است.

## 4. معماری فنی

```text
Browser
  ↓
Next.js Frontend
  ↓
NestJS REST API
  ↓
Prisma ORM
  ↓
SQLite
```

- Monorepo با pnpm workspaces
- Frontend: Next.js، React، TypeScript، App Router
- Backend: NestJS، TypeScript، REST API نسخه‌بندی‌شده
- Database: SQLite با Prisma ORM
- معماری Backend: Modular Monolith
- رابط: Persian-first و RTL
- پورت‌های پیش‌فرض توسعه: frontend روی `3001` و API روی `4001`
- فونت رابط: Vazirmatn Regular به‌صورت self-hosted WOFF2 از مسیر
  `apps/web/public/fonts/Vazirmatn-Regular.woff2`
- استقرار production: یک ابرک Ubuntu 24.04 با Nginx و سرویس‌های systemd؛ API و
  frontend فقط روی loopback و به‌ترتیب پورت‌های `4001` و `3001` اجرا می‌شوند.
- انتشار خودکار: پایش `origin/main` تقریباً هر یک دقیقه، build در release جدا،
  اجرای migration، health check و بازگشت خودکار به release قبلی در صورت شکست.
- داده‌های پایدار production: دیتابیس و upload خارج از Git در
  `/var/lib/cinema-namayesh`؛ بکاپ برنامه روزانه و بکاپ زیرساخت هفتگی.
- شبکه production: فقط SSH، HTTP و HTTPS در UFW باز هستند؛ ورود SSH فقط با
  کلید مجاز است و ورود root با password غیرفعال شده است.
- سایت و API از IP عمومی ابرک در دسترس‌اند. Certbot و تمدید خودکار نصب شده‌اند؛
  صدور TLS و redirect دامنه `ecrannews.ir` تا اتصال DNS هر دو دامنه pending است.

## 5. دامنه داده

مدل‌های پایه:

- User
- Article
- Category
- Tag
- ArticleTag
- MediaAsset
- ArticleSource
- HomepagePlacement

وضعیت انتشار خبر:

```text
DRAFT
IN_REVIEW
SCHEDULED
PUBLISHED
ARCHIVED
```

## 6. تجربه عمومی

مسیرهای اولیه:

```text
/
/category/[slug]
/articles/[slug]
/search
/english
/about
/admin/login
/admin
/admin/articles/new
/admin/articles/[id]/edit
/admin/categories
```

صفحه اصلی با هدف رسیدن سریع‌تر به فوتر، شامل نوار خبر تازه، اسلایدر دستی چهار
خبر مهم، جایگاه‌های معرفی/تبلیغ، شش خبر فشرده از بخش‌های اصلی و لینک‌های عکس،
فیلم و English است. اسلایدر RTL به‌صورت حلقه‌ای با دکمه، نقطه‌های انتخاب و
Drag/Swipe کنترل می‌شود و تپ روی تمام سطح هر اسلاید به صفحه همان خبر می‌رود.
ورودی اسلایدر هنگام transition قفل می‌شود تا کلیک یا swipe سریع track را از
cloneهای ابتدا و انتها عبور ندهد و فضای خالی نمایش داده نشود.
دو خبر «شوتینگا» و «بُت» در ابتدای داده‌ها و اسلایدر قرار دارند.
منوی همبرگری باریک و بدون آیکون فقط در موبایل نمایش داده می‌شود؛ نوارهای رنگی
پیوسته موضوع‌ها را از هم متمایز می‌کنند، زیرمنوی سینما دارد و با
کلیک بیرون، کلید Escape یا انتخاب لینک بسته می‌شود؛ دسکتاپ ناوبری افقی دارد.
Drawer و backdrop از طریق Portal روی `body` رندر می‌شوند تا هنگام بازشدن منو
تمام viewport، از جمله هدر و محتوای صفحه، تیره و محو شود؛ باز و بسته‌شدن هر دو
با transition نرم opacity و حرکت افقی انجام می‌شوند.
هدر از لوگوی محلی `apps/web/public/logo-cinema-namayesh.png` استفاده می‌کند.
قاب لوگو در موبایل و دسکتاپ ارتفاع کافی دارد تا سه نشان بالای «ن» و نوشته پایین
لوگو هم‌زمان و بدون crop نمایش داده شوند.
فوتر علاوه بر لینک مستقیم مجوز، مهر رسمی رسانه را از سرویس `e-rasaneh.ir`
بارگذاری می‌کند.
صفحه «درباره ما» (/about) شامل ماموریت رسانه، استانداردهای سینما نمایش، شفافیت تصویرسازی، پیوند مستقیم گواهی رسمی و کانال‌های ارتباطی (اینستاگرام فعال `cinemanamayesh_ir` و کانال‌های در حال راه‌اندازی تلگرام، یوتیوب و بله) است.
صفحه دسته «سینما» نمای تجمیعی خبرهای مستقیم سینما و چهار زیرمجموعه خبر، نقد و
یادداشت، گفت‌وگو و نمایش است. دسته‌بندی معتبر بدون خبر به‌جای 404 وضعیت خالی
را نمایش می‌دهد؛ 404 فقط برای slug ناشناخته باقی می‌ماند.

## 7. پنل سینما نمایش

MVP فعلی:

- ورود اعضای سینما نمایش با نشست امضاشده در کوکی HttpOnly
- ایجاد، ویرایش، جست‌وجو، فیلتر و بایگانی خبر
- Draft، Review، Schedule، Publish و Archive
- آپلود تصویر از دستگاه، Drag & Drop، کلیپ‌بورد و گالری داخلی
- مدیریت alt، credit، caption، دسته‌بندی، تگ و منابع
- تنظیم SEO و انتخاب/ترتیب خبرهای مهم اسلایدر صفحه اصلی
- رابط responsive و قابل‌استفاده با گوشی
- ساخت یک مدیر production و smoke test کامل ورود، ایجاد خبر منتشرشده، نمایش
  عمومی خبر و قرارگیری آن به‌عنوان چهارمین آیتم اسلایدر انجام شده است.

مراحل بعدی شامل مدیریت اعضای سینما نمایش، محدودیت مجوز دقیق بر اساس role، پیش‌نمایش
امن Draft و مدیریت تبلیغات از داخل پنل است.

## 8. رسانه و SEO

- هر خبر باید slug، title، lead، body، cover image، alt، credit و publishedAt
  داشته باشد.
- صفحات خبر canonical، meta description، Open Graph و JSON-LD از نوع
  `NewsArticle` خواهند داشت.
- فایل‌های نمایشی می‌توانند در Git باشند؛ uploadهای runtime و فایل‌های خصوصی
  نباید commit شوند.
- تصویرهای نمونه قالب در `apps/web/public/images/articles/` محلی هستند و credit
  آن‌ها صریحاً تولید با هوش مصنوعی را اعلام می‌کند؛ این تصاویر سند رویداد واقعی
  محسوب نمی‌شوند.
- تصاویر دو خبر «شوتینگا» و «بُت» از فایل‌های ارائه‌شده برای نمونه استفاده
  می‌کنند و credit نهایی آن‌ها باید پیش از انتشار واقعی توسط سینما نمایش اعلام شود.
- لوگوی عمومی برند در `apps/web/public/logo-cinema-namayesh.png` نگهداری
  می‌شود و برای نمایش آن وابستگی runtime خارجی وجود ندارد.
- مهر مجوز رسانه یک integration خارجی است؛ شناسه گواهی و لینک مقصد باید فقط
  بر اساس اطلاعات رسمی مالک رسانه تغییر کنند.
- storage نهایی رسانه پیش از production باید از filesystem محلی جدا شود.

## 9. محدودیت‌های فعلی

- پایگاه داده SQLite محلی آماده و با داده‌های نمایشی (dummy) مقداردهی اولیه شده است.
- احراز هویت پایه و پنل مدیریت پیاده‌سازی شده‌اند؛ rate limiting، بازیابی رمز،
  audit log و محدودیت عملیاتی دقیق AUTHOR/EDITOR/ADMIN پیش از production لازم‌اند.
- upload فعلی برای توسعه روی filesystem مشترک ذخیره می‌شود و در production باید
  به object storage منتقل شود.
- پیش‌نمایش خصوصی Draft و مدیریت اعضا/تبلیغات هنوز پیاده‌سازی نشده‌اند.
- سیاست editorial، source verification و correction policy نیاز به تأیید دارد.
- لوگوی هدر تأیید و اعمال شده است؛ سایر اجزای هویت بصری هنوز نیازمند تأییدند.
- لوگو و لینک رسمی اکران نیوز و محتوای واقعی تبلیغات هنوز دریافت نشده‌اند.
- رکوردهای DNS دامنه اصلی هنوز به IP ابرک متصل نشده‌اند؛ HTTPS و redirect دائمی
  `ecrannews.ir` پس از propagation رکوردها تکمیل می‌شوند.
- نوار متحرک اخبار با پشتیبانی کامل از RTL، توقف انیمیشن با hover و یکپارچگی مستقیم زیر هدر در صفحه اصلی پیاده‌سازی شده است. اسلایدر حلقه‌ای خبرهای مهم با Drag موس و انگشت، کلیک تمام‌سطحی، شمارنده پایدار RTL و indicator راست‌به‌چپ پیاده‌سازی شده است. جست‌وجوی بازشونده responsive با جایگاه ثابت نسبت به هدر و انیمیشن نرم، ناوبری افقی دسکتاپ و Drawer باریک بدون آیکون با نوار رنگی هر موضوع نیز آماده‌اند.
- اندازه تیتر و برچسب دسته‌بندی در صفحه خبر و صفحه دسته‌بندی متعادل شده و Drop Cap نامناسب برای متن فارسی حذف شده است.

## 10. تست و Definition of Done فاز قالب

- صفحه اصلی، دسته‌بندی و خبر روی دسکتاپ و موبایل خوانا باشند.
- مسیرها build شوند و TypeScript بدون خطا باشد.
- ساختار semantic و metadata اولیه وجود داشته باشد.
- داده عمومی و مدیریتی از API دریافت شود و رکوردهای دستی مبنای نمایش خبر نباشند.
- frontend به Prisma یا PostgreSQL دسترسی مستقیم نداشته باشد.

وضعیت فعلی: تمام موارد بالا برای قالب اولیه برقرارند؛ تأیید بصری کاربر باقی
مانده است.

## 11. Git و ایمنی

- branch فعلی `main` است.
- بدون درخواست صریح push انجام نمی‌شود.
- فایل‌های دقیق stage می‌شوند و از `git add .` استفاده نمی‌شود.
- env، credential، session و uploadهای runtime وارد Git نمی‌شوند.
- repository HotelYab فقط خوانده می‌شود.

## 12. قواعد به‌روزرسانی مستندات

### `PROJECT_CONTEXT.md`

برای تغییر وضعیت محصول، معماری اصلی، مدل داده، routeها، workflow سینما نمایش،
roadmap یا نحوه اجرا.

### `docs/ARCHITECTURE.md`

برای تغییر ساختار سیستم، boundary لایه‌ها، data flow یا استقرار.

### `docs/DECISIONS.md`

برای تصمیم فنی یا محصولی مهم جدید.

### `docs/API.md`

برای تغییر endpoint، query parameter، authentication یا response contract.

### `docs/DATABASE.md`

برای تغییر Prisma model، field، enum، index یا relation.

### `docs/DATA_POLICY.md`

برای تغییر قواعد منبع، راستی‌آزمایی، اصلاح، انتشار یا رسانه.

### `docs/CHANGELOG.md`

پس از هر تغییر مهم و تکمیل‌شده.

### `docs/TODO.md`

هنگام اضافه‌شدن، تکمیل‌شدن یا تغییر اولویت taskها.

## 13. Handoff

برای ادامه کار حداقل `PROJECT_CONTEXT.md` و سپس فایل تخصصی مرتبط از `docs/`
خوانده شود.
